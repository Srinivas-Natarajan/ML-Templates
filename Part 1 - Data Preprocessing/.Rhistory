setwd("D:/Machine Learning A-Z Template Folder/Part 1 - Data Preprocessing")
data=read.csv('Data.csv')
View(data)
View(data)
data$Age = ifelse(test = is.na(data$Age),ave(data$Age,FUN = function(x)mean(x,na.rm=TRUE)),data$Age)
data$Salary = ifelse(test = is.na(data$Salary),ave(data$Salary,FUN = function(x)mean(x,na.rm=TRUE)),data$Salary)
